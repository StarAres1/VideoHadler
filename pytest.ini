[pytest]
testpaths = tests
pythonpath = .
qt_api = pyqt6
# Подробно: каждый тест отдельной строкой (PASSED/FAILED), длинный traceback при ошибках,
# в конце сводка по skip/xfail/passed/failed.
addopts = -vv -ra --tb=long
filterwarnings =
    ignore::DeprecationWarning
